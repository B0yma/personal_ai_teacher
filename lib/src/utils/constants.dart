class AppConstants {
  static const int maxHearts = 100;
  static const Duration heartsRefillTime = Duration(minutes: 1);
  static const int gemsPerLesson = 10; // New constant for gem rewards

  // Set to false to hide developer-specific buttons (View JSON, View Q&A, etc.)
  static const bool isDebugMode = false;
}